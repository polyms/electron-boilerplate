<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Hello Electron React!</title>
  </head>
  <body class="d-flex flex-column position-relative flex-grow-1">
    <div id="root" class="d-flex flex-column position-relative flex-grow-1"></div>
    <script>
      window.electron.ipcRenderer.once('ipc-example', (arg) => {
        // eslint-disable-next-line no-console
        console.log(arg);
      });

      window.electron.ipcRenderer.myPing();
    </script>
  </body>
</html>
